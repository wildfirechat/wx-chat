<view class="container">
    <block wx:for="{{items}}" wx:key="id">
        <view class="item" bindtap="navigateTo" data-url="{{item.url}}">
            <text class="item-text">{{item.name}}</text>
            <text class="item-arrow">></text>
        </view>
    </block>
</view>